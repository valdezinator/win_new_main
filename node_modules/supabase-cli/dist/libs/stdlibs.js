"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.isDirectory = exports.spawn = void 0;
const child_process_1 = __importDefault(require("child_process"));
const fs_1 = require("fs");
const spawn = (command, { params = [], stdio } = {}) => {
    return new Promise((resolve) => {
        const args = command.split(" ").filter((v) => v.length);
        child_process_1.default
            .spawn(args[0], [...args.slice(1), ...params], {
            stdio: stdio ? stdio : "inherit",
        })
            .on("close", (code) => resolve(code));
    });
};
exports.spawn = spawn;
const isDirectory = async (path) => {
    const state = await fs_1.promises.stat(path).catch(() => undefined);
    return state?.isDirectory() === true;
};
exports.isDirectory = isDirectory;
