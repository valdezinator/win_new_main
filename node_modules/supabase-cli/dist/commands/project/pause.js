"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.pause = exports.action = void 0;
const commander_1 = require("commander");
const supabase_1 = require("../../libs/supabase");
const action = async (id, { token, }) => {
    const config = await (0, supabase_1.getRemoteEnv)();
    if (!config)
        return;
    const accessToken = token || config.access_token || "";
    (0, supabase_1.pauseProject)({
        token: accessToken,
        id,
    });
};
exports.action = action;
exports.pause = commander_1.program
    .createCommand("pause")
    .description("Pause a project")
    .addOption(new commander_1.Option("-t, --token <accessToken>", "AccessToken"))
    .addArgument(new commander_1.Argument("id", "Project id").argRequired())
    .action(exports.action);
