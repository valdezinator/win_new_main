"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.list = exports.action = void 0;
const commander_1 = require("commander");
const supabase_1 = require("../../libs/supabase");
const action = async () => {
    const config = await (0, supabase_1.getSupabaseEnv)();
    if (!config)
        return;
    const apiKey = config.SERVICE_ROLE_KEY;
    const port = config.KONG_HTTP_PORT;
    (0, supabase_1.getUsers)({ url: `http://localhost:${port}`, apiKey });
};
exports.action = action;
exports.list = commander_1.program
    .createCommand("list")
    .description("Displaying the user list")
    .action(exports.action);
